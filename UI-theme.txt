
DESIGN_SYSTEM_SCHEMA: "v1.0"
THEME_NAME: "Neon God: Ascension"
AESTHETIC_PROFILE: "Cyberpunk / Retrowave / High-Contrast / Glitch"

# 1. GLOBAL PRIMITIVES (ATOMS)
# ==============================================================================

COLOR_PALETTE:
  # Base Backgrounds
  --bg-void: "#050510"           # Main Canvas/Body Background
  --bg-surface-dark: "#030712"   # Gray-950
  --bg-surface-mid: "#111827"    # Gray-900
  --bg-surface-light: "#1f2937"  # Gray-800
  --bg-overlay: "rgba(0, 0, 0, 0.4)" 
  --bg-glass: "rgba(0, 0, 0, 0.6)"

  # Brand Accents (Neon)
  --neon-cyan: "#22d3ee"         # Cyan-400 (Primary Action / Player)
  --neon-cyan-glow: "rgba(34, 211, 238, 0.5)"
  --neon-fuchsia: "#d946ef"      # Fuchsia-500 (Secondary / Ultimate / Elite)
  --neon-fuchsia-glow: "rgba(217, 70, 239, 0.4)"
  --neon-violet: "#a78bfa"       # Violet-400 (Subtitles)
  --neon-emerald: "#34d399"      # Emerald-400 (Level Up / Success)
  --neon-amber: "#fbbf24"        # Amber-400 (Combo / Gold)
  --neon-red: "#dc2626"          # Red-600 (Danger / Boss / Low HP)

  # Game Entities (Hardcoded Constants)
  --entity-player: "#00f3ff"
  --entity-player-dash: "#ffffff"
  --entity-xp-gem: "#00ffaa"
  --entity-ultimate: "#ff00ff"
  
  # Typography Colors
  --text-primary: "#ffffff"
  --text-secondary: "#9ca3af"    # Gray-400
  --text-muted: "#4b5563"        # Gray-600
  --text-dim: "rgba(255, 255, 255, 0.1)"

TYPOGRAPHY:
  font-stack: "ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace"
  
  weights:
    --fw-regular: 400
    --fw-bold: 700
    --fw-black: 900

  hierarchy:
    display-xl:
      size: "6rem" (8xl) or "4.5rem" (7xl)
      weight: --fw-black
      style: italic
      tracking: tighter (-0.05em)
      effect: drop-shadow-glow
    
    heading-lg:
      size: "3rem" (5xl)
      weight: --fw-black
      tracking: widest (0.1em)
      case: uppercase
    
    heading-md:
      size: "1.875rem" (3xl)
      weight: --fw-bold
      case: uppercase
      tracking: wide
    
    body-mono:
      size: "0.875rem" (sm) or "0.75rem" (xs)
      weight: --fw-bold
      case: uppercase
      tracking: widest (0.2em)

SPACING_AND_LAYOUT:
  # Based on Tailwind 4px grid
  --space-1: "0.25rem"
  --space-2: "0.5rem"
  --space-4: "1rem"
  --space-6: "1.5rem"
  --space-8: "2rem"
  
  --radius-sm: "0.125rem"
  --radius-md: "0.375rem"
  --radius-lg: "0.5rem"
  --radius-xl: "1rem"
  --radius-full: "9999px"

  z-indices:
    --z-background: 0
    --z-canvas: 10
    --z-ui-layer: 60

# 2. SEMANTIC MAPPINGS (THE LOGIC)
# ==============================================================================

VISUAL_EFFECTS:
  # The "Glow" is created via drop-shadows on text and box-shadows on containers
  --glow-text-cyan: "drop-shadow-[0_0_15px_rgba(0,255,255,0.8)]"
  --glow-text-magenta: "drop-shadow-[0_0_15px_rgba(255,0,255,0.8)]"
  --glow-box-cyan: "shadow-[0_0_20px_rgba(34,211,238,0.5)]"
  --backdrop-glass: "backdrop-blur-sm" OR "backdrop-blur-md"

ANIMATION_PRIMITIVES:
  --anim-pulse: "pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite"
  --anim-bounce: "bounce 1s infinite"
  --anim-ping: "ping 1s cubic-bezier(0, 0, 0.2, 1) infinite"
  --transition-fast: "all 150ms ease-in-out"

FUNCTIONAL_COLORS:
  action-primary: --neon-cyan
  action-secondary: --neon-fuchsia
  state-critical: --neon-red (Used for Boss Warnings, Low HP)
  state-success: --neon-emerald (Used for Level Up, XP)
  state-warning: --neon-amber (Used for Combos)

# 3. COMPONENT ARCHITECTURE
# ==============================================================================

COMPONENT: BUTTON_PRIMARY
  structure:
    - container: relative, overflow-hidden, rounded-sm
    - padding: px-16 py-6 (Large click area)
    - bg: transparent (relies on internal absolute divs)
  states:
    default:
      - text: white
      - borders: transparent gradients top/bottom (opacity 0)
    hover:
      - scale: 1.05
      - text: cyan-300
      - bg-overlay: cyan-500/20 (skewed transformation)
      - borders: opacity 100

COMPONENT: CARD_SELECTION (Hull Select / Upgrade)
  structure:
    - container: bg-gray-900, border-gray-700, p-8, rounded-2xl
    - layout: flex-col, center text
  states:
    default:
      - border: 1px solid gray-700
      - shadow: none
    hover:
      - transform: scale-105, translate-y-2
      - border: 1px solid cyan-500 (or emerald-400 for Upgrades)
      - shadow: large glow (cyan/emerald)
      - bg-gradient: internal subtle gradient opacity 100

COMPONENT: HUD_PANEL (Stats / Score)
  structure:
    - bg: black/60 OR black/40
    - border: 1px solid cyan-500/30 OR gray-800
    - backdrop: blur-sm
    - rounding: rounded OR rounded-lg
  content:
    - labels: text-xs, font-bold, tracking-widest
    - values: font-mono, font-black

COMPONENT: PROGRESS_BAR
  structure:
    - track: bg-gray-950, rounded-full OR rounded, overflow-hidden
    - fill: gradient-to-r
  variants:
    - health: from-red-600 to-red-400
    - xp: bg-emerald-500
    - overdrive: from-purple-900 to-purple-600 (Active: bg-fuchsia-500)

COMPONENT: SKILL_ICON
  structure:
    - shape: square (w-12 h-12), rounded-lg
    - border: 2px solid
  states:
    cooldown:
      - border: gray-800
      - bg: black/60
      - opacity: 50%
      - overlay: countdown timer text
    ready:
      - border: cyan-400 (Q) OR fuchsia-400 (E)
      - bg: cyan-900/40 OR fuchsia-900/40
      - shadow: glow
      - cursor: pointer
    active:
      - animation: border ping ring
